# TiGL Examples

TiGL is a C++ library that comes with a graphical user interface, the TiGLViewer, as well as bindings to severall other languages. Checkout the subdirectories for language-specific examples.